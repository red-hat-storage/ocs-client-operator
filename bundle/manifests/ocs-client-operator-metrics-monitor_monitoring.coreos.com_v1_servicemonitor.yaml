apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ocs-client-operator-metrics-monitor
spec:
  endpoints:
  - path: /metrics
    port: https
    tlsConfig:
      insecureSkipVerify: true
  selector:
    matchLabels:
      app: ocs-client-operator
      server: metrics
