apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ocs-client-operator-metrics-monitor
spec:
  endpoints:
  - path: /metrics
    port: metrics
  selector:
    matchLabels:
      app: ocs-client-operator
